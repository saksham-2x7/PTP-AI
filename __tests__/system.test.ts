/* eslint-disable @typescript-eslint/ban-ts-comment */
// @ts-nocheck
import { describe, it, expect } from 'vitest';

describe('MediBridge Core', () => {
  it('initializes correctly', () => {
    expect(true).toBe(true);
  });
});
